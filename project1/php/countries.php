<?php
$use_include_path = "./js/countryBorders.geo.json"
$filename = "countryBorders"

file_get_contents(
    string $filename,
    bool $use_include_path = true,
    resource $context = ?,
    int $offset = 0,
    int $length = ?
): string|false

?>
